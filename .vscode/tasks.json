{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build ARCANEE",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Build the ARCANEE project using CMake"
        }
    ]
}